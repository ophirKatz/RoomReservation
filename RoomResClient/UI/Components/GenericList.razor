@using Data

@typeparam TElement

@if (Elements == null)
{
    <PageLoading />
}
else if (!Elements.Any())
{
    <Alert Type=AlertType.Warning Message="הנתונים נטענים..." />
}
else
{
    @ContentTemplate
}

@code{
    [Parameter]
    public IEnumerable<TElement> Elements { get; set; }

    [Parameter]
    public RenderFragment ContentTemplate { get; set; }
}