@using Data

@typeparam TElement

@if (Elements == null)
{
    <PageLoading />
}
else if (!Elements.Any())
{
    <Alert Type=AlertType.Warning Message="Records not found..." />
}
else
{
    @ContentTemplate
}

@code{
    [Parameter]
    public IEnumerable<TElement> Elements { get; set; }

    [Parameter]
    public RenderFragment ContentTemplate { get; set; }
}