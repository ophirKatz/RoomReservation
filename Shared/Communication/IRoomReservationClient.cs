namespace Shared.Communication
{
    public interface IRoomReservationClient
    {
        // TODO : add server callbacks here.
        // The pattern is : a method to call and an event to listen to. the method raises the event.
    }
}